import React from "react";

const ProjectCard = props => {
    const data = props;
    if (data) {
        console.log("works");
    }
    return (
        <div>
            
        </div>
    );
};

export default ProjectCard;